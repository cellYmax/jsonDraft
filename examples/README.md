# JSON Demo Files

These files are sample inputs for manual testing in jsonDraft.

- `customer-profile.json`: valid object with nested objects and arrays.
- `order-list.json`: valid root array with nested line items.
- `app-config.jsonc`: JSONC sample with comments and trailing commas.
- `invalid-for-error-demo.json`: intentionally invalid JSON for error display testing.
